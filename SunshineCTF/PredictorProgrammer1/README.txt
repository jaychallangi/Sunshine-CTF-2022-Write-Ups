After looking at the code provide it is easy to see what is happening. It takes the seed and generates a guess.
With this information we can copy the way it is doing this (this is what run.py does) and feed it back to the program
to get the flag.
Flag: sun{oops_i_thought_i_was_in_release}